type PhotoType = {
    _id: string,
    country: string,
    county: string,
    date: number,
    device: string,
    download_url: string,
    preview_url: string,
    size: string,
    title: string
}